---
- name: Instalar Python y herramientas adicionales en VM2
  hosts: all
  become: true
  tasks:
    - name: Actualizar cache e instalar Python3, pip y venv
      ansible.builtin.apt:
        name:
          - python3
          - python3-pip
          - python3-venv
        state: present
        update_cache: yes
